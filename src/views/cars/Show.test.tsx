// ETA: 2 hours
describe('<Show />', () => {
  it.skip('should redeirect to 404 if car is not found', () => {
    expect(false).toBe(true);
  });

  // All other tests were moved to "CarDetails.test,tsx"
});
